import { ref, onMounted } from "vue";
import { i as isServer } from "../utils/ssr-utils.mjs";
const useClientOnly = (cb) => {
  if (isServer()) {
    const result = ref(null);
    onMounted(() => {
      result.value = cb();
    });
    return result;
  }
  return ref(cb());
};
export { useClientOnly as u };
//# sourceMappingURL=useClientOnly.mjs.map
